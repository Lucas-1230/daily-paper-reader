---
title: "Structural Inference in Undocumented Mobile Databases: A Reproducible Benchmark for Evaluating Agentic Reasoning in Digital Forensics"
title_zh: 无文档移动数据库中的结构推断：一种用于评估数字取证中智能体推理的可复现基准
authors: "Jeel Piyushkumar Khatiwala, Divyangkumar Patel, Weifeng Xu"
date: 2026-08-21
pdf: "https://arxiv.org/pdf/2608.21470v1"
tags: ["query:ca"]
score: 8.0
evidence: 评估LLM智能体在数字取证中推断无文档移动数据库关系结构的能力，属于网络犯罪调查任务。
tldr: 数字取证中，智能体大模型推断无文档移动数据库关系结构的能力缺乏系统评估，错误推断可能产生看似合理但证据不可靠的结果。本研究使用固定确定性评估管道，对比 Android SMS 与 Snapchat 数据库，考察智能体从原始数据库和自然语言提示重建表关系与连接路径的表现。结果显示规则模式下结构推断可靠，模式不规则时急剧下降，常产生执行成功但结构错误的连接。研究明确了无模式智能推理的适用边界，并强调取证中需额外验证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.21470v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 879, \"height\": 663, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21470v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 657, \"height\": 972, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.21470v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 898, \"height\": 315, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.21470v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1870, \"height\": 995, \"label\": \"Table\"}]"
motivation: 数字取证中，智能体大模型的结构推断能力缺乏独立评估，错误推断可能产生证据不可靠的结果。
method: 使用固定确定性评估管道，对比 Android SMS 与 Snapchat 数据库，以专家验证 SQL 为基准评估结构正确性与执行一致性。
result: 规则模式结构推断可靠，模式模糊时急剧下降，常产生执行成功但结构错误的连接。
conclusion: 无模式智能推理仅适用于规则模式，现实不规则模式下需额外验证才能作为可靠证据。
---

## 摘要
智能体大语言模型在数字取证分析中的应用日益广泛，但它们推断无文档移动应用数据库内部关系结构的能力仍知之甚少。在取证场景中，结构上错误的推断可能产生看似合理但在证据上并不可靠的结果。本研究将智能体结构推断作为一项独立能力进行评估，将执行成功与结构正确性视为不同的评估维度。它考察当仅给定原始数据库和自然语言调查提示时，智能体如何重建表关系、链接属性以及可执行的连接路径。我们将固定的确定性评估流程应用于两个对比鲜明的 SQLite 存储库：具有稳定标识符传播的 Android 短信数据库，以及具有不规则模式、短暂标识符和多态关系的 Snapchat 数据库。使用经专家验证的 SQL 真值，我们评估 (i) 推断关系链接的结构正确性，(ii) 多表推理下的执行一致性，以及 (iii) 当执行成功但关系解释偏离专家真值时推断结构的鲁棒性与失效模式。评估独立于语义解释进行，完整查询和执行轨迹见附录。结果表明，结构推断在常规模式下仍然可靠，但随着模式模糊性增加而急剧退化，频繁产生结构上看似合理但实际错误且能成功执行的连接。这些发现阐明了模式无关的智能体推理在哪些方面可以支持取证分析，其鲁棒性在现实模式不规则下如何退化，以及为何在将推断关系视为可靠证据之前，额外验证仍然至关重要。

## Abstract
Agentic large language models are increasingly used in digital forensic analysis, yet their ability to infer relational structure inside undocumented mobile application databases remains poorly understood. In forensic contexts, structurally incorrect inferences can yield results that appear plausible while remaining evidentially unsound. This work evaluates agentic structural inference as an isolated capability, treating execution success and structural correctness as distinct evaluation axes. It examines how an agent reconstructs table relationships, linking attributes, and executable join paths when given only a raw database and a natural-language investigative prompt. We apply a fixed, deterministic evaluation pipeline to two contrasting SQLite repositories: Android's SMS database with stable identifier propagation, and Snapchat's database with irregular schemas, ephemeral identifiers, and polymorphic relationships. Using expert-verified SQL ground truth, we evaluate (i) structural correctness of inferred relational links, (ii) execution coherence under multi-table reasoning, and (iii) robustness and failure modes of inferred structure when execution succeeds but relational interpretation diverges from expert ground truth. Evaluation is performed independently of semantic interpretation, with full queries and execution traces provided in the Appendix. Results show that structural inference remains reliable in regular schemas but degrades sharply as schema ambiguity increases, frequently producing structurally plausible yet incorrect joins that execute successfully. These findings clarify where schema-agnostic agentic reasoning can support forensic analysis, how its robustness degrades under realistic schema irregularities, and why additional verification remains essential before inferred relationships can be treated as reliable evidence.

---

## 论文详细总结（自动生成）

## 一、论文的核心问题与整体含义

- 研究背景：数字取证中，移动应用数据库（主要是SQLite）常作为核心证据，但应用厂商频繁变更模式、字段和关系设计，且通常没有文档，导致真实数据库在分析时往往只是部分被理解。
- 核心问题：智能体大语言模型在数字取证分析中日益普及，但它们从无文档数据库中推断关系结构的能力尚不清楚。在取证场景中，结构上错误的推断可能产生看似合理但证据上不可靠的结果。
- 整体含义：本研究把“结构推断”作为一项独立能力来评估，明确区分“查询能执行成功”与“推断出的关系结构正确”这两个维度。目标是揭示无模式智能体推理在哪些条件下可靠、哪些条件下退化，并为取证实践提供可复现的基准。

## 二、论文提出的方法论

- 核心思想：将结构推断从语义解释中隔离出来，单独评估智能体在仅获得原始数据库和自然语言调查提示时，如何推断表角色、链接属性、连接路径，并生成可执行SQL。所有推断必须基于模式可见信号，不使用外键、文档、领域语义或外部假设。
- 结构推断定义：从可观测结构信号（跨表标识符重用、列名前缀对齐、时间戳共现模式、表几何相似性）推导候选表、链接属性、连接路径和排序约束的假设构造过程。
- 关键流程：
  1. **模式快照获取**：只读方式枚举表、列、声明数据类型。
  2. **结构假设形成**：基于问题与模式快照推断候选表、链接属性、连接路径、过滤/排序约束。
  3. **SQL 生成**：将假设翻译为恰好一个 SELECT 或 WITH 查询，仅可引用模式中已定义的表和列，必要时补 LIMIT。
  4. **模式感知验证与受监督执行**：验证未知表/列等结构矛盾，在只读沙箱中执行有效查询，记录结果与错误轨迹。
- 智能体配置：使用 OpenAI GPT-4.1（2025年1月版），温度固定为0.0、top-p为1.0，输入输出token预算均为4096；每个任务只触发一次推理循环，无重试、多种子采样、提示调整或微调。
- 评估指标：
  - **结构键与行级一致性**：对每个问题，将真实查询和智能体生成查询的结果投影到专家定义的关键列集合上，计算 Precision、Recall、F1。公式为：  
    \( P_q = \{\pi_{K_q}(r) \mid r \in R_q^{AG}\} \)，\( G_q = \{\pi_{K_q}(r) \mid r \in R_q^{GT}\} \)；  
    Precision = \(|P_q \cap G_q| / |P_q|\)，Recall = \(|P_q \cap G_q| / |G_q|\)。
  - **执行一致性（execution coherence）**：二进制指标，表示生成的SQL是否通过验证并成功执行而不产生结构矛盾。
  - **鲁棒性与失败模式**：仅针对执行成功的查询，分析推断结构与专家真值的背离及原因。

## 三、实验设计

- 数据集/场景：
  - **Android SMS 数据库（mmssms.db）**：规则模式，具有稳定标识符传播、明确线程语义、统一时间戳。
  - **Snapchat 数据库（main.db）**：不规则模式，短暂标识符、多态表、辅助缓存表、不一致命名，关系间接或弱传播。
- 基准任务：
  - 共12个调查问题，分为三个难度层级：G1基础检索（单表/直接过滤）、G2上下文过滤（有限关系链）、G3多阶段/对抗性任务（隐式键恢复、多表重建、时间漂移、缺失检测等）。
  - 每个问题都有专家验证的 SQL 真值，由两名具有5年以上经验的移动取证分析师独立构建并达成共识。
- 对比方法：本研究不是对比多个模型，而是在同一智能体配置下，对比规则模式与不规则模式、不同任务难度下的表现。评估独立于语义解释，只看结构正确性、执行一致性、执行成功但结构错误的鲁棒性。

## 四、资源与算力

- 论文未明确说明所使用的 GPU 型号、数量、训练时长或推理时消耗的算力。
- 可以确定的是：本研究不使用本地训练，而是调用 OpenAI GPT-4.1 模型进行推理；每个任务的输入输出 token 预算固定为4096，温度0.0、top-p 1.0，每个问题仅单次执行。
- 因此，在“资源与算力”方面，论文缺乏具体硬件和计算成本披露。

## 五、实验数量与充分性

- 主要实验规模：2个数据库 × 12个任务 = 24个主要评估实例；再加上每个任务的结果投影、执行一致性分组、失败模式案例分析。
- 充分性评估：
  - 覆盖了两个对比鲜明的模式环境，具有控制性对比价值。
  - 但每个难度层级任务数较少（G1/G2/G3各4个），总体上更像一个受控探针而非大规模基准。
  - 单一模型、单一提示策略、单次确定性运行，缺少多模型、多提示、随机采样下的统计方差分析。
  - 专家真值只有一个专家验证路径，未系统纳入可能存在的其他等价SQL解释。
- 客观性与公平性：
  - 使用固定确定性管道、专家验证SQL、行级投影指标，减少了主观干扰。
  - 但由于仅采用一个模型且温度0，结果无法反映随机解码下的稳定性，也不能直接推广到其他模型家族。

## 六、论文的主要结论与发现

- RQ1（结构链接恢复）：在规则模式 mmssms.db 中，智能体能可靠恢复G1/G2任务的关系路径，F1接近满分；在 Snapchat main.db 中，一旦需要跨表推断，性能急剧下降，出现关键字段混淆（如 conversation_id 与 container_id 混淆），假阳性与假阴性同时增加。
- RQ2（执行稳定性）：SMS数据库在G1/G2执行一致性高，失败集中在G3；Snapchat在需要隐式键恢复或多表重建时执行一致性显著降低，出现属性丢失、连接不兼容、路径断裂。
- RQ3（执行成功但结构错误）：在成功执行的查询中，仍然频繁出现结构背离。归纳出四类失效模式：
  1. 歧义链接属性选择；
  2. 连接路径替换；
  3. 辅助表干扰；
  4. 结构过度约束。
- 总体结论：执行成功不等于结构正确。规则模式下结构推断可作早期探索；不规则模式下，推断关系必须经过额外验证才能作为可靠证据。提出“分级信任模型”：模式规则、标识符一致、关系距离短时可暂信；否则仅视为需佐证的假设。

## 七、优点

- 问题定位清晰：把“结构推断”从语义理解中分离出来，解决了以往只关注查询可执行性或表面准确性的缺陷。
- 可复现性强：固定智能体配置、确定性参数、完整SQL轨迹和评估代码公开在GitHub。
- 双重评估轴：区分“执行成功”与“结构正确”，能够暴露那些可执行但证据错误的查询，这在取证中具有重要意义。
- 系统性失败模式分析：归纳了四类可操作的失效原因，并提出对应的验证动作，对实践有指导价值。
- 使用真实数据库和专家真值，增强了生态效度。

## 八、不足与局限

- 单一模型配置：仅使用GPT-4.1且固定提示策略，限制跨模型、参数规模和提示设计的推广性。
- 确定性单次运行：没有置信区间、随机解码统计，无法刻画稳定性分布。
- 基准范围有限：仅2个真实数据库、12个任务，不能代表移动应用模式的全貌；小任务数更适合作为探针而非人口级估计。
- 专家真值与替代查询问题：只承认一个专家验证路径，结构上不同但证据等价的其他查询可能被错误扣分，影响对低一致性结果的解释。
- 缺少跨数据库推理与多源重建：每个数据存储独立分析，未覆盖真实取证中跨应用、跨系统关联。
- 未披露算力细节：GPU、推理成本等未说明。
- 应用限制：仅涉及SQLite；未讨论云端、非SQLite或混合存储；未评估提示优化对结果的影响。

（完）
