---
title: "Big Enough to Break Out: Tracking the Rising Capability of LLM Penetration-Testing Agents"
title_zh: 大到足以突围：追踪 LLM 渗透测试代理的上升能力
authors: "Victoria Lovelace, Cameron Berryman, Yuhan You, Suhas Reddy Adavelly, Joel Sadler, Daniel Graham"
date: 2026-09-09
pdf: "https://arxiv.org/pdf/2609.10780v1"
tags: ["query:ca"]
score: 9.0
evidence: 追踪LLM渗透测试智能体的能力，展示自主系统解决所有三个目标
tldr: 大型语言模型渗透测试代理能力快速提升，但其失败机理与瓶颈尚不清楚。本研究对比了基于Kimi K2.5的人工在环系统与基于Claude Opus 4.8的自主PentestGPT系统，在三个公开目标上测试，并引入覆盖记忆层检验记忆假设。自主系统解决全部三个目标，遗留系统完成约一半子任务却未攻克两个目标；添加记忆层未改善结果，表明规划能力而非记忆是主要限制。该子任务评分方法可供防御者持续监测此类能力演进，提前预警。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.10780v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1077, \"height\": 795, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.10780v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 987, \"height\": 342, \"label\": \"Table\"}]"
motivation: 探究LLM渗透测试代理的能力边界与失败原因，明确长任务中的限制因素。
method: 对比Kimi K2.5人工在环与Claude Opus 4.8自主PentestGPT系统，在三个公开目标上测试，并添加覆盖记忆层。
result: 自主系统解决全部三个目标，遗留系统完成约一半子任务但未完成两个目标；覆盖记忆层无提升，主要限制为规划而非记忆。
conclusion: LLM渗透测试代理的限制瓶颈在于规划能力而非记忆，子任务评分方法可供防御者持续监测能力上升并预警。
---

## 摘要
大语言模型（LLM）代理正越来越多地被应用于渗透测试，但我们对它们能做什么以及如何失败仍知之甚少。我们比较了两个基于 PentestGPT 的系统：一个较旧的人在回路系统，运行开放权重的 Kimi K2.5；另一个是较新的自主系统，运行 Claude Opus 4.8。在三个公开目标上，自主系统全部完成，包括旧系统从未完成的两个。旧系统的结果更令人意外。即使在旧系统未能攻克的机器上，它也完成了大约一半的子任务，并且是在普通大学 GPU 上运行，没有提供商护栏。我们能够描述这一趋势，却无法解释它，因为模型、执行框架、自主性和记忆架构都同时发生了变化。这一趋势仍指向下一个问题：当这些代理承担更复杂的任务时，什么会限制它们？通常的答案是长程记忆，即在长攻击链中丢失了对早期发现的访问。我们通过向两个系统添加覆盖记忆层来检验这一点，但两者的结果都没有改善。在我们能够审查的旧系统停滞运行中，限制因素似乎是规划与投入，而非记忆丢失：代理已经掌握了前进路径的证据，却从未将其转化为具体的利用假设，这可能表明攻击能力将随着代理的规划能力而非记忆的改善而提升。用于追踪这一能力的相同子任务评分对防御者也是可用的，他们可以在能力上升时加以测量，而不必等到在实战中遭遇它。

## Abstract
Large language model (LLM) agents are increasingly applied to penetration testing, but we still know little about what they can do or how they fail. We compare two PentestGPT-based systems: a legacy human-in-the-loop system running the open-weight Kimi K2.5, and a newer autonomous system running Claude Opus 4.8. Across three public targets, the autonomous system solves all three, including the two the legacy system never finishes. The legacy result is the more surprising of the two. Even on the machines the legacy system fails to solve, it completes about half the subtasks, while running on ordinary university GPUs with no provider guardrails. We can describe the trend but not explain it, since model, harness, autonomy, and memory architecture all change together. Its direction still points to the next question: what will limit these agents as they take on more complex tasks? The usual answer is long-horizon memory, the loss of access to earlier findings during long attack chains. We test it by adding a coverage-memory layer to both systems, and neither improves outcomes. In the legacy stalled runs we could review, the limiting factor appeared to be planning and commitment rather than lost memory: agents held the evidence for a route forward and never turned it into a concrete exploitation hypothesis, which may suggest that offensive capability will advance with agents' ability to plan rather than with better memory. The same subtask scoring that tracks this capability is available to defenders, who can measure it as it rises instead of waiting to meet it in the field.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究背景**：大语言模型（LLM）代理正被越来越多地应用于渗透测试，但对其实际能力边界和失败机制仍缺乏系统认知。早期工作（如 PentestGPT）发现 LLM 能处理特定子任务，但难以维持长程攻击链的整体上下文。
- **核心问题**：随着 LLM 渗透测试代理能力快速上升，什么因素会成为它们处理更复杂任务时的瓶颈？通常的假设是“长程记忆丢失”，但本文试图检验该假设是否成立，并更精确地定位限制因素。
- **整体含义**：论文以“投影导向”的方式，测量能力轨迹上的两个点——旧的人在回路系统和新的自主系统——并利用二者差距及剩余失败模式，推断能力的发展方向。其结论对防御者尤其重要：与其在实战中被动遭遇，不如主动测量和追踪这些代理的上升能力。

## 2. 论文提出的方法论

- **两个 PentestGPT 系统对比**：
  - **Legacy（旧系统）**：基于原始 PentestGPT 架构，使用 Kimi K2.5 开放权重模型。包含三个模型会话：推理模块（维护 Pentesting Task Tree，PTT）、生成模块（将任务转为命令）、解析模块（压缩工具输出）。人类操作员仅执行命令并返回输出，不参与规划或纠错。
  - **Autonomous（自主系统）**：基于维护中的 pentestgpt_agent 框架，使用 Claude Opus 4.8。包含两个模型角色：Supervisor（读取运行状态投影，选择任务或提议完成）和 Executor（执行任务并返回类型化结果）。一个确定性控制器管理循环，验证计划、租借任务、调用 Executor、验证证据并提交状态。权威记录存储在 SQLite Memory Kernel 中，模型提供者会话不保留跨会话状态。
- **Coverage-Memory 干预**：
  - 设计目标：检验“持续访问早期发现能否减少重复工作”。
  - 实现方式：两个版本都包含存储（记录已检查的攻击面）和召回（在规划前将笔记提供给规划器）两部分，但集成方式不同。
  - **自主系统实现**：写入是自动的，代码在每次观察提交到 kernel 时复制到 vault，无模型调用、无重复；读取是 Supervisor 的主动动作，可在提议发现/枚举任务前检查 vault。Executor 不能访问 vault。
  - **旧系统实现**：采用类似 Karpathy’s LLM Wiki 的模式，在推理会话内部维护 wiki。读取是确定性的；但每次更新需要额外的推理模型调用，因此旧系统的干预同时改变了可见信息和模型工作量。
- **评测指标**：
  - 结局分类：成功、停滞（stall）、生成失败、中止（aborted）。
  - 子任务评分：针对 Bob 和 Tr0ll 使用固定、目标特定的 14 步子任务清单，仅在转录中看到命令输出或直接证据时才给分，不依赖模型自身声明。
  - 效率指标：推理周期或 Supervisor 决策数、推理时间、token 使用、成本、墙钟时间。
  - 失败模式分类：上下文丢失、错误命令生成、错误输出解释、未能识别解决路径、幻觉发现、固着（fixation）。

## 3. 实验设计

- **评测目标（数据集/场景）**：
  - 三个公开漏洞机器：Metasploitable 2、Bob、Tr0ll。其中 Bob 和 Tr0ll 有清晰的攻击链和固定子任务清单；Metasploitable 2 有多个独立漏洞服务，无单一攻击链，因此只比较效率。
  - 第四个目标 HackTheBox Enigma 仅用于探索性运行，不使用固定清单评分。
- **Benchmark 与对比方法**：
  - 每个系统都在 baseline 和 coverage-memory 两种条件下运行。
  - Legacy 系统：baseline 和 coverage-memory 条件；模型为 Kimi K2.5。
  - Autonomous 系统：baseline 和 coverage-memory 条件；模型为 Claude Opus 4.8。
  - 另外有 exploratory Enigma 运行，使用 Claude Opus 5，仅 baseline（coverage 关闭）。
- **试验协议**：
  - 每项试验以固定提示开始，提供机器名、操作系统、IP 和目标，但不提供攻击计划。
  - 成功证明要求经过验证的 root 权限、指定证明文件内容或远程代码执行的直接证据。
  - 代理不能使用端到端漏洞扫描器（如 OpenVAS、Nessus），但可以在找到特定攻击面后使用针对性工具（如 sqlmap、公开的 CVE 利用）。
  - Legacy 会话修剪到约 40 条消息，命令超时 30 分钟；Autonomous 受框架决策和尝试限制，无墙钟超时。

## 4. 资源与算力

- **Legacy 系统**：通过 University of Virginia Research Computing GenAI 服务访问 Kimi K2.5；命令从 Kali Linux 虚拟机运行。文中提到“普通大学 GPU”，但**未给出具体 GPU 型号、数量或运行时长**。推理时间以秒计，但未说明硬件细节。无提供商护栏，无计费成本。
- **Autonomous 系统**：使用 Claude Opus 4.8（通过 Anthropic 商业 API），reasoning effort 设为 xhigh。文中报告了每次运行的成本（美元）和 token 使用量，例如 Metasploitable 2 平均成本约 $0.82（baseline）和 $0.79（coverage）。**未提供 Anthropic 后端的具体算力细节**，因为这是云 API 推理，不是本地训练或推理。
- **总结**：论文未说明训练算力（因为不涉及训练）；推理阶段的硬件细节对 autonomous 系统不适用，对 legacy 系统也仅模糊提及“普通大学 GPU”，缺乏具体型号和数量。

## 5. 实验数量与充分性

- **实验组数**：
  - Legacy baseline：Metasploitable 2 5 次，Bob 1 次，Tr0ll 2 次。
  - Legacy coverage-memory：仅 Metasploitable 2 5 次。
  - Autonomous baseline：Metasploitable 2 3 次，Bob 3 次，Tr0ll 3 次。
  - Autonomous coverage-memory：Metasploitable 2 2 次，Bob 3 次，Tr0ll 3 次。
  - Exploratory Enigma：3 次（Claude Opus 5，baseline，coverage 关闭）。
- **充分性与客观性评价**：
  - **样本量极小**：每个条件通常只有 1–5 次试验，无法进行统计推断；论文也明确承认这一点，仅报告均值和范围。
  - **混淆严重**：两个系统在模型、框架、执行范式、自主性和记忆架构上全部不同，无法隔离单一变量，因此不能归因于模型或框架。
  - **基准污染风险**：所有目标均有公开 walkthrough，可能出现在训练数据中；即使子任务评分和转录审查能显示进度，也无法区分“推理得出”与“记忆复现”。
  - **控制器设置不完全一致**：不同目标的决策/尝试上限不同；Enigma 使用不同模型和更高预算，且不属于受控比较。
  - **Memory 干预前提未触发**：在受控比较的自主试验中，重复发现为零，因此 coverage-memory 从未真正被测试到；这使得该实验更多是“证明不需要记忆”而非“证明记忆无效”。
  - **评分依赖人工判断**：虽然论文保留了转录片段以便复查，但某些判断（如是否属于同一方法）不可避免带有主观性。

## 6. 论文的主要结论与发现

- **能力差距显著**：自主 Claude 系统在 Metasploitable 2、Bob、Tr0ll 上全部完成攻击；legacy Kimi 系统仅解决 Metasploitable 2，在 Bob 和 Tr0ll 上停滞，但在这两个未解决的机器上仍完成了约一半子任务（Bob 7/14，Tr0ll 9/14 和 7/14）。
- **Coverage-memory 未改善结果**：两个系统加入覆盖记忆层后均未提高成功率；legacy 版本的推理时间每周期增加约 2.9 倍；autonomous 版本 Supervisor 输出增加约 18%，但总 token 使用接近基线。
- **失败主因可能不是记忆丢失，而是规划/投入不足**：
  - 在受审查的 legacy 停滞试验中，固着（fixation）出现在所有三项中；代理反复改变同一策略而不改变总体方法。
  - 转录中已经包含通往成功路径的证据，但代理未能将其转化为具体的利用假设并采取行动。
  - 在 Enigma 探索性运行中，所有三次都耗尽决策预算但未获得 root；任务剖面以枚举和测试为主，没有或极少利用任务，出现“过度分解”模式，这与规划问题而非记忆丢失一致。
- **对防御者的启示**：防御者可以在实验室中运行类似配置（开放权重模型 + 普通硬件），通过子任务清单和失败模式分析，追踪能力上升，而不必等到实战遭遇。

## 7. 优点

- **直接检验流行假设**：针对“长程记忆是瓶颈”的常见说法，设计了 coverage-memory 干预，并通过实验否定了其在当前目标上的有效性。
- **细粒度评测**：除了成功/失败二分类，还引入子任务清单、重复发现计数、失败模式分类和效率指标，能更细致地刻画代理行为。
- **失败分析深入**：通过转录审查，将停滞归因于规划/投入而非记忆丢失，提供了有价值的定性证据。
- **防御视角**：强调同样的评测方法可用于防御者主动监测能力上升，具有实际安全意义。
- **透明披露局限**：论文对混淆、样本量、基准污染等威胁进行了详细说明，不夸大结论。

## 8. 不足与局限

- **样本量过小**：每个条件下的试验次数太少，无法进行统计推断，效率差异（如 Tr0ll 决策数减少 30%）无法与随机波动区分。
- **无法归因**：两个系统在所有维度上都不同，不能确定能力差距来自模型、框架、自主性还是记忆架构；未能在同一个框架下比较不同模型。
- **基准污染**：公开目标可能已经被模型记忆，无法证明代理真正进行了推理；未来需要无公开 walkthrough 的定制目标。
- **Memory 干预未真正测试**：在受控比较的目标上，重复发现为零，记忆丢失的前提从未发生，因此 coverage-memory 的无效性不能推广到更长任务；需要更长目标来触发记忆丢失。
- **Legacy coverage-memory 实验范围有限**：仅在 Metasploitable 2 上运行，未覆盖 Bob 和 Tr0ll；且其实现同时改变了可见信息和模型工作量，无法分离成本来源。
- **探索性 Enigma 运行受控性差**：使用不同模型（Claude Opus 5）、更高预算、有主机崩溃和回滚，不能作为正式证据。
- **人为判断成分**：失败模式和是否“同一方法”的评分存在主观性，尽管保留了转录片段以供复查，但缺乏独立评分。
- **控制器设置不一致**：不同目标的决策/尝试上限不同，跨目标效率比较无效。

（完）
