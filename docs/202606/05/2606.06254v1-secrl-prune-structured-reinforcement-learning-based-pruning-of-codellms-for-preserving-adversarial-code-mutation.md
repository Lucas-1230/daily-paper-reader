---
title: "SecRL-Prune: Structured Reinforcement Learning-Based Pruning of CodeLLMs for Preserving Adversarial Code Mutation"
title_zh: SecRL-Prune：基于结构化强化学习的代码大语言模型剪枝以保留对抗性代码变异能力
authors: "Parsa Memarzadehsaghezi, Pooria Madani, Khalil El-Khatib"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06254v1"
tags: ["query:ca"]
score: 10.0
evidence: 保留代码LLM的对抗性代码变异能力，直接关联恶意软件免杀
tldr: "代码语言大模型(CodeLLMs)能够生成功能保持的代码突变，可能被用于制造恶意软件变种以规避检测。模型压缩是否会导致该突变能力丧失，是一个未被充分探索的安全问题。本文提出SecRL-Prune，一个基于强化学习的结构化剪枝框架，针对性剪除前馈网络通道，并以教师-学生KL散度作为奖励，配合top-P缓存提高训练效率。在HumanEval上对三个7B代码模型评估显示，在高达30%的压缩率下，我们方法在代码执行正确率和多样性上均优于近期剪枝基线。真实恶意软件案例中，仅20%压缩的模型就能生成大幅降低杀毒软件检测率的语义保持变种。这一发现表明，压缩后的代码大模型仍具备强大的对抗性代码生成能力，对模型轻量化部署的安全策略敲响警钟。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06254v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 899, \"height\": 330, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06254v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 872, \"height\": 589, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06254v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 356, \"height\": 621, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06254v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1559, \"height\": 447, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06254v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1559, \"height\": 504, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06254v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 870, \"height\": 566, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06254v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 756, \"height\": 864, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06254v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1519, \"height\": 536, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06254v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1850, \"height\": 587, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06254v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 890, \"height\": 956, \"label\": \"Table\"}]"
motivation: 探究CodeLLMs经过结构化剪枝压缩后，其生成对抗性代码突变的能力是否依然存在，这对安全部署和资源受限场景极为重要。
method: 采用强化学习为每层前馈通道学习剪枝策略，以教师-学生KL散度为奖励，并缓存教师top-P预测，避免同时加载两个模型。
result: "在10-30%压缩率下，SecRL-Prune的代码执行正确率和多样性均高于现有方法；20%剪枝模型生成的恶意变种使检测率骤降。"
conclusion: 即使经过显著结构化剪枝，压缩后的代码大模型仍能产生有效的对抗性突变，对安全构成实质性威胁，需引起关注。
---

## 摘要
大型代码语言模型（CodeLLM）能够生成和重写程序，实现功能保持的代码变异，这可能被用于创建多样化的恶意软件变体并逃避基于签名的检测。一个关键的安全问题是这种变异能力能否在模型压缩后幸存，从而在有限硬件预算下实现部署。我们提出SecRL-Prune，一个针对CodeLLM的结构化剪枝框架，作用于前馈（MLP/FFN）通道。从预训练的教师模型出发，利用教师-学生KL散度奖励，通过强化学习学习逐层剪枝策略。为提高效率，我们一次缓存教师的top-P预测，并将剪枝后的学生模型与这一紧凑目标进行比较，避免教师和学生同时驻留在GPU内存中。我们在HumanEval上使用pass@k评估执行正确性，使用var@k评估代码多样性，对三个7B CodeLLM在10-30%压缩率下进行评估。SecRL-Prune在激进剪枝下始终比最新的结构化剪枝基线保持更高的pass@k和var@k。在一个真实恶意软件样本的案例研究中，来自20%剪枝模型的语义保持变异显著降低了检测率。这些结果表明，代码变异能力可以在显著的结构化剪枝后幸存，突显了压缩CodeLLM的安全相关性。

## Abstract
Large code language models (CodeLLMs) can generate and rewrite programs, enabling functionality-preserving code mutation that may be used to create diverse malware variants and evade signature-based detection. A key security question is whether this mutation capability survives model compression, which would make deployment feasible under limited hardware budgets. We propose SecRL-Prune, a structured pruning framework for CodeLLMs that operates on feed-forward (MLP/FFN) channels. Starting from a pretrained teacher, it learns a layer-wise pruning policy with reinforcement learning using a teacher-student KL-divergence reward. To improve efficiency, we cache the teacher's top-P predictions once and compare the pruned student against this compact target, avoiding simultaneous teacher-student residency in GPU memory. We evaluate SecRL-Prune on HumanEval using pass@k for execution correctness and var@k for code diversity across three 7B CodeLLMs at 10-30% compression. SecRL-Prune consistently preserves higher pass@k and var@k than recent structured pruning baselines under aggressive pruning. In a case study on real malware samples, semantics-preserving mutations from 20%-pruned models substantially reduced detections. These results show that code mutation capability can survive significant structured pruning, highlighting the security relevance of compressed CodeLLMs.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究背景**：大型代码语言模型（CodeLLMs）具备强大的代码生成与重写能力，可被用于生成功能保持的代码突变，从而制造多形态恶意软件变种以逃避基于签名的静态检测。  
- **核心问题**：如果对 CodeLLM 进行模型压缩（如剪枝、蒸馏、量化），其代码变异能力是否会丧失？若该能力能在显著压缩后幸存，则攻击者有可能将紧凑的“变异引擎”嵌入恶意载荷或部署于消费级硬件，构成现实威胁。  
- **整体含义**：本文首次系统性地探究了结构化剪枝对 CodeLLM 代码变异保真度的影响，揭示了压缩后模型仍可保留较强的对抗性代码生成能力，强调了这一攻击向量的安全风险。

### 2. 论文提出的方法论：核心思想、关键技术细节、流程
- **核心思想**：SecRL-Prune 是一种基于强化学习（RL）的结构化剪枝框架，针对 Transformer 块中前馈网络（FFN/MLP）的中间通道进行剪枝，以教师模型的输出分布为引导，学习最优通道保留策略。  
- **关键技术细节**：
  - **剪枝单元**：以每层 MLP 的通道（神经元）为基本单位。剪枝时同时移除该通道对应的第一线性层行权重和第二线性层列权重。  
  - **教师输出缓存**：为降低内存开销，先在标定集上运行一遍教师模型，仅缓存每一个时间步的前 \\(P\\) 个最高概率的 token 及其 logit，其余概率合并为一个 “Other” 桶，形成紧凑的 \\((P+1)\\) 维目标分布。  
  - **策略设计**：为每一层维护一个可学习向量 \\(\pi^l \in \mathbb{R}^{d^l_{\text{ff}}}\\)，通过 softmax 转换为概率分布，按目标压缩比采样得到二进制掩码 \\(m^l\\)，决定哪些通道保留。  
  - **奖励函数**：基于剪枝后学生模型与缓存教师分布的 KL 散度（计算仅在前 \\(P\\) 个 token 和 Other 桶上），取负期望损失作为奖励。  
  - **训练算法**：使用 REINFORCE 策略梯度更新通道得分，最终选取奖励最高的掩码作为结果，对学生模型实施物理剪枝。  
- **流程概要**：标定集 → 教师前传缓存 top‑\\(P\\) logits → 策略采样生成掩码 → 学生前传并计算 KL 奖励 → 更新策略 → 选出最优掩码 → 物理移除冗余通道得到压缩模型。

### 3. 实验设计：数据集、基准、对比方法
- **数据集与场景**：
  - **校准集**：300 条混合自 MBPP 和 GitHub 函数的 prompt（仅包含自然语言描述与函数签名，不含单元测试）。  
  - **评测基准**：HumanEval（Python 程序合成），使用 pass@k 衡量执行正确性，var@k 衡量正确解中的代码多样性（变异能力）。  
  - **安全案例**：3 个来自 MalwareBazaar 的真实恶意软件样本（2 个 Python, 1 个 JavaScript），通过 VirusTotal 对比原始文件与突变文件的检出率。
- **对比方法**：
  - 无压缩的稠密教师模型（上界）。  
  - PruneNet（新的策略学习结构化剪枝基线），它本身已优于早期结构化剪枝方法。  
  - SecRL-Prune 的自身消融（不同压缩比 10%/20%/25%/30%）。
- **评估指标**：pass@1/10/100 的正确率保留百分比（Pres.）；var@10/100 的多样性保留百分比；恶意软件检出率降低幅（ΔD）。

### 4. 资源与算力
- **硬件**：实验均在 NVIDIA A100 40 GB GPU 上运行。  
- **内存优化**：SecRL-Prune 通过缓存教师 top‑\\(P\\) logits，在策略训练时仅需加载学生模型；对于 20% 压缩比，峰值 GPU 内存从 PruneNet 的 33‑37 GiB 降至 14‑16 GiB，降幅达 55‑60%。  
- **训练细节**：batch size 300，学习率 \\(3\times10^{-4}\\)，每个压缩比 10 次策略更新，但文中未明确给出具体的总训练时长或 GPU 卡数，仅说明了每个配置取 5 次随机种子的均值。

### 5. 实验数量与充分性
- **实验组数**：
  - 3 个模型 × 4 个压缩比 × 2 种方法（SecRL-Prune 与 PruneNet）+ 1 个稠密教师，共 \\((3\times4\times2) + 3 = 27\\) 组配置，每组 5 个随机种子取平均。  
  - 额外案例研究：3 个恶意软件样本 × 2 个 20% 剪枝模型，共 6 个突变测试。  
- **充分性分析**：  
  - 覆盖了主流 CodeLLM（CodeLlama-7B‑Instruct、CodeLlama-7B‑Python、Qwen2.5‑Coder‑7B‑Instruct），具有代表性。  
  - 压缩比从轻量 10% 到激进 30%，能够观察能力衰减趋势。  
  - 对比基线单一（仅 PruneNet），未与其他经典结构化剪枝（如 LLM‑Pruner）或蒸馏方法直接比较，但所选的 PruneNet 被视为强基线。  
  - 校准集大小（300）和 prompt 来源较有限，可能影响泛化评估。整体实验设计合理，能支撑结论，但对比方法和评测多样性仍有扩展空间。

### 6. 论文的主要结论与发现
- **性能保留显著优于基线**：在所有压缩比下，SecRL-Prune 的正确性保留率（pass@k）均大幅领先 PruneNet；30% 压缩时，其保留百分比约为 PruneNet 的 2 倍（如 Qwen2.5‑Coder 上 63.7% vs. 35.7%）。  
- **代码变异能力依然存在**：即使在 30% 压缩下，剪枝模型仍能产生多种不同的正确解法（var@k 下降相对平缓），表明通道选择有效捕获了对多样性至关重要的 MLP 容量。  
- **压缩模型可降低恶意软件检测率**：经 20% 压缩模型的一次函数级突变，VirusTotal 检出率最多可下降 88%（JavaScript 样本），验证了紧凑变异引擎的现实威胁。  
- **训练内存大幅减少**：得益于 top‑\\(P\\) 缓存，SecRL-Prune 在策略优化时的峰值显存需求降低超 50%，使低资源环境下的剪枝更为可行。

### 7. 优点：方法或实验设计上的亮点
- **安全视角新颖**：首次关注剪枝后 CodeLLM 的代码变异能力保留，而非仅评测通用代码生成，直面恶意软件演化这一实际问题。  
- **训练高效**：top‑\\(P\\) 缓存策略避免同时加载教师和学生模型，显著降低显存开销，并保持与全词表蒸馏相近的指导质量。  
- **策略学习直观**：直接为每个 MLP 通道分配可学习分数，通过 RL 搜索最优子集，无需依赖手工设计的通道重要性指标。  
- **实验证据扎实**：同时评估了编程基准（HumanEval）和真实恶意软件检出，从正确性、多样性到实际安全影响形成了完整证据链。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **校准集与评测局限**：校准 prompt 仅 300 条、来源有限；评测仅使用 HumanEval 单一基准，未在更多编程语言或复杂场景下验证，可能高估泛化性能。  
- **对比方法单一**：仅与 PruneNet 对比，未纳入其他主流结构化剪枝（LLM‑Pruner）或蒸馏/量化方法，不能说明 SecRL-Prune 在所有压缩技术中的优势。  
- **奖励设计**：KL 散度仅基于 top‑\\(P\\) 分布，可能忽略长尾行为或精确 token 匹配；此外，奖励依赖于标定集的分布，压缩后的适应能力可能受限。  
- **剪枝范围受限**：仅剪枝 FFN 通道，未处理自注意力部分（如头、键/值维度），而注意力也占据大量参数；未来可探索联合剪枝。  
- **对抗安全性评估简化**：恶意软件实验为点对点案例，样本量小（3 个）、突变次数少（每个仅一次），且 VirusTotal 检出为某一时间点的快照，难以反映持久性免杀效果。  
- **未考虑微调恢复**：剪枝后未经有监督微调或知识蒸馏的进一步恢复，性能与稠密教师仍有明显差距，实用部署可能需结合多阶段精调。

（完）
