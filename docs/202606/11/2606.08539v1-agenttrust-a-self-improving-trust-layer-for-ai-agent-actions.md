---
title: "AgentTrust: A Self-Improving Trust Layer for AI-Agent Actions"
title_zh: "AgentTrust: 面向AI代理行为的自改进信任层"
authors: Chenglin Yang
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08539v1"
tags: ["query:ca"]
score: 8.0
evidence: 为AI智能体行为设计的信任层区分词汇与语义威胁，直接应对LLM智能体的安全问题。
tldr: "AI代理需信任层判断操作安全性，但传统固定规则无法应对表面相似但意图不同的语义威胁。AgentTrust提出按威胁类型区分的双存储系统：LLM法官自学习，为词汇威胁蒸馏确定性规则，为语义威胁构建带验证的RAG记忆。规则准确率从48%提升至83.6-85.2%，语义准确率提高13个百分点，在线重放中零错误阻止良性操作，法官调用率下降而准确率上升，实现自进化、低成本、高安全的信任层。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08539v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1695, \"height\": 693, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08539v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1671, \"height\": 824, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08539v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1537, \"height\": 712, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08539v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1649, \"height\": 1095, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08539v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1649, \"height\": 1108, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08539v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1758, \"height\": 746, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08539v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1755, \"height\": 397, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08539v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1756, \"height\": 309, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08539v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1757, \"height\": 593, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08539v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1757, \"height\": 430, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08539v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1755, \"height\": 390, \"label\": \"Table\"}]"
motivation: 现有信任机制依赖固定规则，对同形异义的语义威胁无效，手工规则包在语义类别上准确率无提升。
method: 构建双存储自改进系统：LLM法官蒸馏词汇规则形成确定性层，并为语义威胁构建带验证的RAG记忆，结合自学习与先例。
result: "规则准确率近乎翻倍（48%→83.6-85.2%），语义准确率提升13pp，在线45000次操作中零错误阻止良性行为，法官调用率从50%降至44%。"
conclusion: AgentTrust v2实现按威胁类型自进化的信任层，词汇类成本降低，语义类准确性提升，从未错误阻止良性操作，为AI代理安全提供高效方案。
---

## 摘要
AI代理越来越多地执行关键操作——shell命令、云操作和任意工具调用——因此信任层必须针对每个操作决定是允许、警告、阻止还是上报。我们认为，推理这种信任层的正确方式是按威胁类型分类。词法（固定签名）威胁，危险存在于稳定的符号中，可通过确定性规则判定；语义（意图依赖）威胁，其中良性操作和恶意操作表面相同，在构造上超出了规则的范围。我们通过一个否定性证明具体化了这一点：一个精心手工编写的云规则包将保留测试的准确率仅从48%提升到56%，并且语义类别的提升为0个百分点（data_db从29到29，observability从59到59，supply_chain从50到50），而强大的LLM裁判正是覆盖了这些类别。我们赋予该裁判自学习能力：在一个主要包含语义攻击的语料库上，它将规则准确率几乎翻倍（从48%提升到83.6-85.2%），且几乎零误阻断，这一结果在两个模型提供商上均成立。我们将其转化为一个自改进的双存储系统：裁判将不断增长的确定性规则层提炼到词法威胁上（随时间推移成本更低），并在语义威胁上馈入一个受保护的RAG记忆（直接使用判决缓存失败——表面孪生导致准确率下降到约58%——因此一个佐证守卫将语义准确率提升了13个百分点，从70到84）。这正是AgentTrust v2区别于其静态v1前身之处：一个从自身决策流中自我进化的信任层——在词法类别上更经济（它提炼自己的规则），在语义类别上更智能（它积累受保护的先例），同时从不硬性阻止良性操作。端到端在线重放显示，裁判调用率下降（从50%下降到44%），裁判域准确率上升（从71%上升到80%），在45,000次操作中实现了0次良性硬阻断。

## Abstract
AI agents increasingly take consequential actions -- shell commands, cloud operations, and arbitrary tool-calls -- so a trust layer must decide, per action, whether to allow, warn, block, or escalate. We argue that the right way to reason about such a layer is by threat type. Lexical (fixed-signature) threats, where danger lives in a stable token, are decidable by deterministic rules; semantic (intent-dependent) threats, where a benign and a malicious action share the same surface, are out of reach for rules by construction. We make this concrete with a negative proof: a determined, hand-authored cloud rule pack lifts held-out accuracy only 48 to 56% overall and moves the semantic categories by 0pp (data_db 29 to 29, observability 59 to 59, supply_chain 50 to 50), while a strong LLM judge carries exactly those categories. We give the judge a self-learning capability: on a corpus that is mainly semantic attacks it nearly doubles rule accuracy (48% to 83.6-85.2%) with near-zero false-blocks, and this holds across two model providers. We turn this into a self-improving dual-store system: the judge distills a growing deterministic rule floor on lexical threats (cheaper over time) and feeds a guarded RAG memory on semantic threats (a verdict-cache fails -- surface-twins collapse to ~58% -- so a corroboration guard lifts semantic accuracy +13pp, 70 to 84). The result is what sets AgentTrust v2 apart from its static v1 predecessor: a trust layer that self-evolves from its own stream of decisions -- cheaper on the lexical class (it distils its own rules) and smarter on the semantic class (it accrues guarded precedent), while never hard-blocking a benign action. An end-to-end online replay shows the judge-call rate falling (50% to 44%) and judge-domain accuracy rising (71% to 80%), with 0 benign hard-blocks across 45,000 actions.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机与背景**：随着AI代理越来越多地执行高风险操作（如shell命令、云API调用），需要一种“信任层”对每一个操作进行实时裁决（允许/警告/阻止/上报）。现有方法主要依赖固定的确定性策略（正则表达式、AST匹配等），但它们只能处理危险存在固定符号的“词法威胁”，例如 `rm -rf /` 或硬编码密钥；而无法处理“语义威胁”——即恶意操作与良性操作表面完全相同、仅意图不同的情况（如一个正常的 curl 命令可能是遥测，也可能是数据外泄）。该论文旨在证明语义类威胁是固定规则在构造上无法逾越的，并以此为基础设计一个能够自我进化的信任层，将规则的廉价性与大语言模型（LLM）法官的推理能力结合起来。

### 2. 论文提出的方法论
- **核心思想**：将威胁严格分解为“词法（固定签名）威胁”和“语义（意图依赖）威胁”，并据此构建**双存储自改进系统**。词法威胁用不断增长的确定性规则地板处理；语义威胁由带置信门控的LLM法官处理，并辅以受保护的检索增强生成（RAG）记忆。系统通过自身的判决流不断学习：法官将新的词法签名提炼为确定性规则以降低成本，同时将经过佐证验证的语义判例存入记忆以提升准确性。
- **关键技术细节**：
  - **确定性规则地板**：继承v1的ShellNormalizer（去混淆）和170+条规则，负责识别固定签名威胁，快速且可审计。
  - **置信门控语义法官**：对于规则无法判断的操作，由LLM法官判断意图并输出裁决和置信度。仅当置信度高于阈值τ（因模型而异）时，才允许法官的提升裁决（如允许→警告/阻止/上报），以此抑制误报。
  - **受保护的RAG记忆**：语义类威胁无法通过表面缓存解决（表面孪生问题），因此法官的判例被存入记忆，在新操作到来时检索相似判例作为上下文辅助法官推理，而非直接复用判决。为防止记忆中毒，采用“佐证守卫”：只有两个独立法官（Sonnet-4.6与Opus-4.8）意见一致时，该判例才被存入记忆。
  - **自改进回路**：
    - **词法→规则提炼**：当法官发现新的词法签名威胁，将其蒸馏为确定性规则，后续相同表面直接由地板处理，降低法官调用频率。
    - **语义→受保护记忆**：经过佐证守卫的语义判决写入RAG记忆，提升法官在语义类任务上的准确率。
  - **融合策略**：根据流量分布选择策略——在规则可信的分布内使用“规则地板+仅在置信时提升”的混合；在分布外则让法官主导（门控替换），规则仅作建议。
  - **安全不变量**：系统永不会将良性操作硬阻断；最坏情况下仅发出软性警告或请求人工审核。

### 3. 实验设计
- **数据集**：
  1. **内部300**：调优语料库，用于开发阈值和规则，不用于核心声明。
  2. **独立630**：未参与调优的独立测试集，代表分布内（与规则设计分布一致）。
  3. **外部保留189**：真实、分布外（OOD）语料，经独立第二标注员重标注且通过泄露检查，Cohen的κ=0.82。该语料被有意设计为攻击场景丰富（52%为语义攻击），包含8个威胁类别（如shell_persistence, data_db, observability_benign_hard等）。
- **基准（对比方法）**：
  - **规则配置**：诚实默认规则（170条）、欺骗性规则（含针对特定资源的优化，仅测试上限）、默认+云规则包（手工编写的新规则，作为“更多规则”的钢人论证）。
  - **法官配置**：五个LLM配置，跨两个提供商（Anthropic: Haiku-4.5, Sonnet-4.6, Opus-4.8；OpenAI: GPT-5.5）以及一个弱基线DeepSeek。
- **评估指标**：精确裁决准确率、假阳性率（FPR）、假阴性率（FNR）、良性硬阻断计数（安全不变量）。

### 4. 资源与算力
- 论文未明确说明实验所用GPU型号、数量或训练时长。实验是对静态语料库的重放模拟，法官调用为API推理，不涉及模型训练；因此未提供算力细节。

### 5. 实验数量与充分性
- **实验组数**：论文涉及多组实验，覆盖：
  - 3个数据集上所有法官与规则配置的对比（表1-3）。
  - 不同融合策略（混合vs法官主导）的对比。
  - 按威胁类别的性能分解（表3）。
  - 语义记忆模块的消融研究（缓存失效分析，RAG带/不带佐证守卫的对比，表4）。
  - 端到端在线重放模拟（30个流，共45,000次操作，表5）。
- **充分性与客观性**：实验设计较为全面，使用多个分布、多个模型提供商进行验证，包含消融实验和负面结果（证明缓存失败），并通过独立重标注确保外部测试集客观性。但语料库是人工策划的，并非真实生产流量，在线循环为模拟而非实时运行。

### 6. 论文的主要结论与发现
1.  **语义威胁是结构性的，无法通过增加规则解决**：即使手工编写更多规则，也只能提升词法类准确率，对语义类别提升为0pp，且调优到极限的规则集在OOD上仍只有47.6%准确率。
2.  **法官是泛化的关键组件**：在主要含语义攻击的OOD语料上，规则准确率崩溃至48.1%，而强LLM法官（Opus-4.8等）达到83.6-85.2%，几乎零误阻断，且结果跨提供商一致。
3.  **自改进双存储系统有效**：
    - **更经济**：词法规则提炼使法官调用率从50%降至44%。
    - **更智能**：带佐证守卫的RAG记忆将语义准确率提升13个百分点（70%→84%），记忆正确率从70%升至97%。
    - **始终安全**：在全部实验中，包括45,000次操作的重放，均未出现良性操作被硬阻断。
4.  **安全不变量成立**：系统最坏情况下的误判仅导致软性警告，绝不会硬阻断良性操作。

### 7. 优点
- **问题分解深刻**：提出的词法/语义威胁二分法及构造性局限的证明，为信任层设计提供了清晰的理论基础。
- **系统设计精巧**：将LLM法官作为“教师”，分别通过蒸馏和记忆提升规则和自身能力，实现了成本递减与智能递增的双重进化。
- **实验扎实且具反直觉发现**：证明了语义缓存不可行（表面孪生导致精准度无安全区域），并展示了佐证守卫对记忆安全的关键作用。
- **跨模型、跨分布验证**：结果在多个模型和两个提供商上保持稳定，增加了结论的可靠性和泛化性。
- **严守安全底线**：以零良性硬阻断作为设计基石，提升了系统的可部署性。

### 8. 不足与局限
- **非生产环境评估**：所有评估均基于人工策划的语料库和重放模拟，未在真实生产流量、并发、对抗性适应等条件下验证，外推至真实环境的绝对准确率可能不同。
- **在线循环为模拟**：自改进循环并非实时运行和重新裁决，而是基于固定测量结果的模拟，未能测试在线标签噪声和反馈循环可能带来的风险。
- **成本摊销幅度保守**：由于语料库故意偏向攻击和语义类，法官调用率下降幅度有限（50%→44%），真实良性主导的流量中可能更显著，但未进行量化。
- **记忆守卫单一且昂贵**：语义记忆的最佳结果依赖于两个法官佐证的昂贵守卫，仅评估了此一种守卫变体，更经济的守卫策略（如单法官高置信度门控）未被探索。
- **门阈值手动设定**：置信度阈值是手工校准的且依赖模型提供者，需在新模型上重新调参，缺少自动校准机制。
- **样本量有限**：外部测试集仅189个操作（语义切片98个），类别精度可能存在较大置信区间。

（完）
